export const COLORS = ["red", "green", "yellow", "blue", "purple", "grey", "black", "pink", "orange"];
export enum TIME {
    always,
    clear,
}
export enum PATH_COLORS {
    hint = "pink",
    drawed = "grey",
}
